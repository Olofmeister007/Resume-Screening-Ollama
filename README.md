# Resume-Screening-Ollama
This simple python script screens resumes based on a provided description using ollama (running deepseek locally) and it ranks the resumes from 0 to 10 (poor match to excellent match). It also provides the reasoning behind the rating.
